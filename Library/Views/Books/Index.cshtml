@{
  Layout = "_Layout";
}

@using Library.Models;
@model List<Library.Models.Book>;
<div class="row">
  <div class="col-md-6 offset-md-3">

    <h1 class="text-center">Books</h1>

    @if (@Model.Count == 0)
    {
      <h3 class="text-center">No categories have been added yet!</h3>
    } 
    <div class="list-group" id="list-tab" role="tablist">
      @foreach (Book book in Model)
      {
        <div class="list-group-item list-group-item-action">
          📚 @Html.ActionLink($"{book.Title}", "Details", new { id = book.BookId })
        </div>
      }
    </div>
    <br>
    <br>
    <div class="text-center">
    @Html.ActionLink("➕", "Create", null, null, new {@class="btn btn-success"})

    @Html.ActionLink("🏠", "Index", "Home", null, new {@class="btn btn-info"})
  </div>
  </div>
</div>