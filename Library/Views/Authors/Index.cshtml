@{
  Layout = "_Layout";
}

@using Library.Models;
@model List<Library.Models.Author>;
<div class="row">
  <div class="col-md-6 offset-md-3">
      <h1 class="text-center">Authors</h1>

      @if (@Model.Count == 0)
      {
        <h3>No categories have been added yet!</h3>
      } 
      <div class="list-group" id="list-tab" role="tablist">
      @foreach (Author author in Model)
      {
        <div class="list-group-item list-group-item-action">
          ✏️ @Html.ActionLink($"{author.Name}", "Details", new { id = author.AuthorId })
        </div>
      }
      </div>
      <div class="text-center">

        @Html.ActionLink("➕", "Create", null, null, new { @class="btn btn-success"})
        @Html.ActionLink("🏠", "Index", "Home", null, new { @class="btn btn-info"})
      </div>
      </div>

  </div>
</div>